using System;

namespace SpaceClaim.AddInLibrary {
    public static class Const {
        public const double Tau = Math.PI * 2;
        public static readonly double Phi = (1 + Math.Sqrt(5)) / 2;

        public const double inches = 0.0254;
        public const double feet = inches / 12;
    }
}
